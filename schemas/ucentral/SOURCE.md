# OLG/uCentral Schema Source

Place the actual source schema files here:

```text
schemas/ucentral/schema.json
schemas/ucentral/ucentral.schema.full.json
```

Recommended source for MVP:

```text
repository: https://github.com/Telecominfraproject/olg-ucentral-schema/blob/ra_proposal
ref:       pin to a tag or commit before release
files:
  - schema.json
  - ucentral.schema.full.json
```

Use a branch only during active development. Use a tag or commit SHA for CI/release builds.
